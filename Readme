Basic tool for calculating subnet related fields from an ip (with or without cidr) such as: Subnet Mask, Network/Broadcast ID, IP Range.
Runs interactively and otherwise accepts arguments or piping.

Use makefile to install and uninstall.
